import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    'ikusi-green': '#006b54',
    'ikusi-teal': '#0094a8',
    'ikusi-dark': '#002e24',
  },
});

export default theme;
